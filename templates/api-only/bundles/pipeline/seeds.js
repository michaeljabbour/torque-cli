/**
 * Per-bundle seed for pipeline (api-only template).
 * Stub — deals are seeded from the global seeds/index.js.
 */

export function seedPipeline(pipeline, ctx) {
  // Pipeline seeding is handled in seeds/index.js
}
