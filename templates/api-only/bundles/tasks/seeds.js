/**
 * Per-bundle seed for tasks (api-only template).
 * Stub — tasks are seeded from the global seeds/index.js.
 */

export function seedTasks(tasks, ctx) {
  // Task seeding is handled in seeds/index.js
}
